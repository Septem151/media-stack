"""Internal package for the media-stack CLI (entrypoint: scripts/stack).

Modules: lib (shared helpers), host (root setup), render (config + certs),
wiring (app API bootstrap), lifecycle (up/down/restart/setup)."""
